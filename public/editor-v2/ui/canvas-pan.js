const canvas=document.querySelector('#canvas');
const panButton=document.querySelector('#panTool');
if(canvas&&panButton){let active=false;let x=0;let y=0;let left=0;let top=0;panButton.addEventListener('click',()=>{canvas.style.cursor='grab'});canvas.addEventListener('pointerdown',event=>{if(!panButton.classList.contains('active'))return;active=true;x=event.clientX;y=event.clientY;left=canvas.scrollLeft;top=canvas.scrollTop;canvas.style.cursor='grabbing';canvas.setPointerCapture(event.pointerId)});canvas.addEventListener('pointermove',event=>{if(!active)return;canvas.scrollLeft=left-(event.clientX-x);canvas.scrollTop=top-(event.clientY-y)});const stop=event=>{if(!active)return;active=false;canvas.style.cursor='grab';try{canvas.releasePointerCapture(event.pointerId)}catch{}};canvas.addEventListener('pointerup',stop);canvas.addEventListener('pointercancel',stop)}
