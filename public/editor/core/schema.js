/* Sanci9517 Visual Editor — canonical document schema. */
(function(){
  const VERSION=2;
  const TYPES=new Set(['root','section','container','row','columns','grid','stack','flex','group','text','heading','paragraph','richtext','link','button','image','video','audio','icon','svg','divider','spacer','badge','avatar','card','list','table','tabs','accordion','dropdown','breadcrumb','pagination','search','countdown','progress','notification','cookie','modal','navbar','menu','footer','sidebar','backtotop','form','input','textarea','checkbox','radio','select','slider','file','submit','gallery','carousel','embed','iframe','map','code','socialembed','live','twitch','youtube','tiktok','discord','schedule','streamcount','followers','subs','vod','support','community','gamecard','gamelist','calendar','component','repeater','query','custom','customcss','script','popup','condition']);
  const uid=(prefix='node')=>`${prefix}-${crypto.randomUUID()}`;
  const clone=value=>value==null?value:JSON.parse(JSON.stringify(value));
  function emptyNode(type='container',extra={}){return {id:extra.id||uid(type),type,name:extra.name||type,parentId:extra.parentId||null,children:Array.isArray(extra.children)?extra.children:[],content:extra.content||{},layout:extra.layout||{},style:extra.style||{},responsive:extra.responsive||{desktop:{},tablet:{},mobile:{}},interaction:extra.interaction||{},visibility:extra.visibility!==false,locked:extra.locked===true,metadata:extra.metadata||{},dataBindings:extra.dataBindings||{},capabilities:extra.capabilities||{}};}
  function emptyPage(page={}){const rootId=uid('root');return {id:page.id||uid('page'),name:page.name||page.title||'Új oldal',slug:page.slug||'uj-oldal',metadata:page.metadata||{},settings:page.settings||{},responsive:page.responsive||{desktop:{},tablet:{},mobile:{}},root:{id:rootId,type:'root',name:'Oldal',parentId:null,children:[]}};}
  function emptyDocument(page={}){const first=emptyPage(page);return {schemaVersion:VERSION,type:'sanci-document',pages:[first],activePageId:first.id};}
  function normalizeDocument(document){if(document?.pages?.length)return document;if(document?.page?.id){const page=clone(document.page);return {schemaVersion:VERSION,type:'sanci-document',pages:[page],activePageId:page.id};}return emptyDocument();}
  function activePage(document){const doc=normalizeDocument(document);return doc.pages.find(p=>p.id===doc.activePageId)||doc.pages[0]||null;}
  function walk(node,fn){if(!node)return;fn(node);(node.children||[]).forEach(child=>walk(child,fn));}
  function index(document){const map=new Map();const page=activePage(document);walk(page?.root,n=>map.set(n.id,n));return map;}
  function validatePage(page,errors,warnings){if(!page?.id)errors.push('Hiányzó page ID.');if(!page?.root?.id)errors.push('Hiányzó root ID.');const seen=new Set(),parents=new Map();walk(page?.root,n=>{if(seen.has(n.id))errors.push(`Duplikált ID: ${n.id}`);else seen.add(n.id);if(!TYPES.has(n.type))warnings.push(`Ismeretlen elem típus: ${n.type}`);(n.children||[]).forEach(child=>{if(parents.has(child.id))errors.push(`Egy elemnek több parentje van: ${child.id}`);parents.set(child.id,n.id);if(child.parentId!==n.id)errors.push(`Hibás parentId: ${child.id}`);});});}
  function validate(document){const errors=[],warnings=[];if(!document||document.type!=='sanci-document')errors.push('Érvénytelen dokumentumtípus.');const doc=normalizeDocument(document);if(!Array.isArray(doc.pages)||!doc.pages.length)errors.push('Legalább egy oldal szükséges.');const ids=new Set();(doc.pages||[]).forEach(p=>{if(ids.has(p.id))errors.push(`Duplikált page ID: ${p.id}`);ids.add(p.id);validatePage(p,errors,warnings)});if(!doc.activePageId||!ids.has(doc.activePageId))errors.push('Érvénytelen aktív oldal.');return {valid:errors.length===0,errors,warnings};}
  window.SanciEditorSchema={VERSION,TYPES,uid,clone,emptyNode,emptyPage,emptyDocument,normalizeDocument,activePage,walk,index,validate};
})();
