(()=>{
const getGroup=()=>{if(!state.selected?.length)return null;const ns=state.selected.map(find).filter(Boolean),gs=[...new Set(ns.map(n=>n.groupId).filter(Boolean))];if(gs.length!==1)return null;const all=allNodes().filter(n=>n.groupId===gs[0]);return all.length===ns.length?{id:gs[0],all}:null};
const box=()=>{const g=getGroup();if(!g)return null;let x=Infinity,y=Infinity,r=-Infinity,b=-Infinity;g.all.forEach(n=>{const l=n.layout||{};x=Math.min(x,Number(l.x)||0);y=Math.min(y,Number(l.y)||0);r=Math.max(r,(Number(l.x)||0)+(Number(l.width)||0));b=Math.max(b,(Number(l.y)||0)+(Number(l.height)||0));});return {...g,x,y,width:r-x,height:b-y};};
const f=(label,key,v,type='text')=>`<div class="field"><label>${label}</label><input data-gkey="${key}" type="${type}" value="${String(v??'').replaceAll('"','&quot;')}"></div>`;
const old=renderInspector;
window.renderInspector=()=>{const g=box();if(!g){old();return}const styles=g.all.map(n=>n.style||{}),same=k=>[...new Set(styles.map(s=>s[k]))].length===1?[...new Set(styles.map(s=>s[k]))][0]:'';const locked=g.all.every(n=>n.locked),visible=g.all.every(n=>n.visible!==false);const i=$('inspector');i.innerHTML=`<details class="inspect-section" open><summary>Csoport tulajdonságai</summary><div class="inspect-body">${f('Név','name','Csoport')}<div class="field-row">${f('X','x',Math.round(g.x),'number')}${f('Y','y',Math.round(g.y),'number')}</div><div class="field-row">${f('Szélesség','width',Math.round(g.width),'number')}${f('Magasság','height',Math.round(g.height),'number')}</div><div class="hint">${g.all.length} elem a csoportban.</div><label class="check"><input data-gkey="locked" type="checkbox" ${locked?'checked':''}> Zárolás</label><label class="check"><input data-gkey="visible" type="checkbox" ${visible?'checked':''}> Látható</label></div></details><details class="inspect-section" open><summary>Megjelenés</summary><div class="inspect-body">${f('Szín','color',same('color'))}${f('Háttér','background',same('background'))}${f('Szegély','border',same('border'))}${f('Lekerekítés','radius',same('radius'),'number')}${f('Átlátszóság','opacity',same('opacity'),'number')}</div></details>`;i.querySelectorAll('[data-gkey]').forEach(e=>e.addEventListener('change',()=>{const k=e.dataset.gkey,v=e.type==='checkbox'?e.checked:e.type==='number'?Number(e.value):e.value;commit();if(k==='name'){g.all.forEach(n=>n.name=v)}else if(k==='x'||k==='y'){const d=(k==='x'?v:g.x)-(k==='x'?g.x:g.y);g.all.forEach(n=>n.layout[k]=(Number(n.layout[k])||0)+d)}else if(k==='width'||k==='height'){const s=v/(k==='width'?g.width:g.height);g.all.forEach(n=>{n.layout[k]=Math.max(20,(Number(n.layout[k])||20)*s)})}else if(k==='locked'||k==='visible'){g.all.forEach(n=>n[k]=v)}else{g.all.forEach(n=>{n.style=n.style||{};n.style[k]=v})}render()})})};
})();
