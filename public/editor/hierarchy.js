(()=>{
  const api=()=>window.SanciEditor||(window.SanciEditor={state:window.state,find:window.find,commit:window.commit,render:window.render,renderCanvas:window.renderCanvas,renderInspector:window.renderInspector,updateSelectionState:window.updateSelectionState,show:window.show,clearSelection:window.clearSelection});
  const containerTypes=new Set(['root','section','container','row','columns','grid','stack','flex','group','card']);
  const esc=v=>String(v).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
  const getSelected=()=>{const a=api();return a?.state?.selected?.map(id=>a.find(id)).filter(Boolean)||[]};
  const isContainer=n=>!!n&&containerTypes.has(n.type);
  const walk=(nodes,fn)=>{for(const n of nodes||[]){fn(n);walk(n.children||[],fn)}};
  const parentOf=(id,nodes=api()?.state?.doc?.root?.children)=>{for(const n of nodes||[]){if((n.children||[]).some(c=>c.id===id))return n;const found=parentOf(id,n.children||[]);if(found)return found}return null};
  const hasDescendant=(ancestorId,targetId)=>{const a=api(),ancestor=a?.find?.(ancestorId);if(!ancestor)return false;let hit=false;walk(ancestor.children||[],n=>{if(n.id===targetId)hit=true});return hit};
  const removeFromTree=(id,nodes)=>{for(let i=0;i<(nodes||[]).length;i++){if(nodes[i].id===id)return nodes.splice(i,1)[0];const hit=removeFromTree(id,nodes[i].children||[]);if(hit)return hit}return null};
  const absolutePosition=(n,parent)=>{let x=Number(n.layout?.x)||0,y=Number(n.layout?.y)||0,p=parent;while(p){x+=Number(p.layout?.x)||0;y+=Number(p.layout?.y)||0;p=parentOf(p.id)}return{x,y}};
  const addChildren=()=>{const a=api(),sel=getSelected();if(!a||!a.state.doc||sel.length<2){a?.show?.('Jelöld ki a szülőt és legalább egy gyermekelemet');return}const parent=sel[0],children=sel.slice(1);if(!isContainer(parent)){a.show('Az első kijelölt elemnek konténernek kell lennie');return}if(children.some(n=>n.id===parent.id||hasDescendant(n.id,parent.id))){a.show('Érvénytelen hierarchia: körkörös kapcsolat');return}a.commit();parent.children=parent.children||[];children.forEach(child=>{const oldParent=parentOf(child.id),removed=removeFromTree(child.id,a.state.doc.root.children);if(!removed)return;const abs=absolutePosition(removed,oldParent);removed.layout=removed.layout||{};removed.layout.x=abs.x-(Number(parent.layout?.x)||0);removed.layout.y=abs.y-(Number(parent.layout?.y)||0);parent.children.push(removed)});a.state.selected=[parent.id];a.render();a.show(`${children.length} elem a(z) „${parent.name}” gyermekévé vált`)};
  const moveOut=()=>{const a=api(),sel=getSelected();if(!a||!a.state.doc||sel.length!==1){a?.show?.('Egyetlen gyermekelemet jelölj ki');return}const child=sel[0],parent=parentOf(child.id);if(!parent){a.show('Ez az elem már a gyökérszinten van');return}const grand=parentOf(parent.id);a.commit();const removed=removeFromTree(child.id,parent.children||[]);if(!removed)return;const abs=absolutePosition(removed,parent);removed.layout=removed.layout||{};removed.layout.x=abs.x-(Number(grand?.layout?.x)||0);removed.layout.y=abs.y-(Number(grand?.layout?.y)||0);(grand?grand.children:a.state.doc.root.children).push(removed);a.state.selected=[removed.id];a.render();a.show('Elem kiemelve a szülőből')};
  const renderLayerTree=()=>{const a=api(),box=document.getElementById('layers');if(!a||!box||!a.state?.doc)return;box.innerHTML='<div class="panel-head">Oldal szerkezete</div>';const make=(n,depth=0)=>{const wrap=document.createElement('div');wrap.className='layer-node';const d=document.createElement('div');d.className='layer hierarchy-layer';d.dataset.nodeId=n.id;d.style.paddingLeft=(10+depth*18)+'px';if(a.state.selected.includes(n.id))d.classList.add('selected');const has=!!n.children?.length;d.innerHTML=`<span class="layer-name"><span class="hierarchy-arrow">${has?'▾':'•'}</span>${n.locked?'🔒 ':''}${esc(n.name)}</span><span>${esc(n.type)}</span>`;d.onclick=e=>{e.preventDefault();e.stopPropagation();a.state.selected=(e.ctrlKey||e.metaKey)?(a.state.selected.includes(n.id)?a.state.selected.filter(x=>x!==n.id):[...a.state.selected,n.id]):[n.id];a.renderCanvas();a.renderInspector();renderLayerTree();a.updateSelectionState?.()};wrap.appendChild(d);if(has){const kids=document.createElement('div');kids.className='layer-children';n.children.forEach(ch=>kids.appendChild(make(ch,depth+1)));wrap.appendChild(kids)}return wrap};(a.state.doc.root.children||[]).forEach(n=>box.appendChild(make(n,0)))};
  const installCanvasSelection=()=>{const canvas=document.getElementById('canvas');if(!canvas||canvas.dataset.hierarchySelectionReady==='1')return;canvas.dataset.hierarchySelectionReady='1';const arm=()=>canvas.querySelectorAll('.node').forEach(el=>{if(el.dataset.hierarchyChildReady==='1')return;el.dataset.hierarchyChildReady='1';el.addEventListener('pointerdown',e=>{if(e.currentTarget!==e.target)e.stopPropagation()})});arm();new MutationObserver(arm).observe(canvas,{childList:true,subtree:true})};
  const boot=()=>{const toolbar=document.querySelector('.canvas-tools');if(!toolbar)return;const a=api();if(document.getElementById('nestBtn'))document.getElementById('nestBtn').onclick=addChildren;if(document.getElementById('unnestBtn'))document.getElementById('unnestBtn').onclick=moveOut;window.SanciHierarchy={parentOf,moveToParent:(childId,parentId)=>{const child=a?.find?.(childId),parent=a?.find?.(parentId);if(!child||!parent||child.id===parent.id||!isContainer(parent)||hasDescendant(child.id,parent.id))return false;const oldParent=parentOf(child.id),removed=removeFromTree(child.id,a.state.doc.root.children);if(!removed)return false;const abs=absolutePosition(removed,oldParent);removed.layout=removed.layout||{};removed.layout.x=abs.x-(Number(parent.layout?.x)||0);removed.layout.y=abs.y-(Number(parent.layout?.y)||0);parent.children=parent.children||[];parent.children.push(removed);return true},isContainer,hasDescendant,renderLayerTree};window.renderLayers=renderLayerTree;installCanvasSelection();renderLayerTree()};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();