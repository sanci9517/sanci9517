const menuButton=document.querySelector('[data-menu-button]');const menuPanel=document.querySelector('[data-menu-panel]');if(menuButton&&menuPanel){const setOpen=open=>{menuPanel.classList.toggle('open',open);menuButton.setAttribute('aria-expanded',String(open));menuButton.textContent=open?'✕':'☰'};menuButton.addEventListener('click',()=>setOpen(!menuPanel.classList.contains('open')));menuPanel.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setOpen(false)));document.addEventListener('click',e=>{if(!menuPanel.contains(e.target)&&!menuButton.contains(e.target))setOpen(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')setOpen(false)})}const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();async function loadSiteSettings(){try{const r=await fetch('/api/public/site-settings',{headers:{Accept:'application/json'},cache:'no-store'});if(!r.ok)return;const s=await r.json();document.querySelectorAll('[data-site-name]').forEach(el=>{if(typeof s.siteName==='string'&&s.siteName.trim())el.textContent=s.siteName.trim()});document.querySelectorAll('[data-site-tagline]').forEach(el=>{if(typeof s.tagline==='string')el.textContent=s.tagline})}catch{}}loadSiteSettings();